{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "@jscutlery/semver:github",
  "description": "",
  "type": "object",
  "properties": {
    "tag": {
      "type": "string",
      "description": "Create a new release from given tag."
    },
    "branch": {
      "type": "string",
      "description": "Target branch or full commit SHA (default: main branch)."
    },
    "files": {
      "type": "array",
      "description": "A list of asset files may be given to upload to the new release."
    }
  },
  "required": ["tag"]
}
