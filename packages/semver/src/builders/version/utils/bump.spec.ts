describe('bump', () => {
  it.todo(
    '🚧 should compute next version based on current version and changes'
  );

  it.todo('🚧 should return null if there are no changes in current path');
});
