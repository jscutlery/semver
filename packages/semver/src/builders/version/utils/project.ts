import { JsonObject } from '@angular-devkit/core';
import { Observable } from 'rxjs';

export function readPackageJson({ projectRoot }): Observable<JsonObject> {
  throw new Error('🚧 work in progress!');
}
